<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user_path(user) %></li>
    
    <ul>
      <% user.items.each do |item| %>
        <li> Item: <%= image_tag item.photo.url(:thumb) %></li>
      <% end %>
    </ul>
  <% end %>
</ul>