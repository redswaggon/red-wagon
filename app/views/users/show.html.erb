<h2>Hello <%= @user.name %>!</h2>

<h3>Here is your info.</h3>
<%= link_to "Troll for wagons, you addict.", random_wagon_nearby_path(username: @user.username) %><br>
Username: <span class="username"><%= @user.username %></span><br>
<%= @user.email %><br>
<%= @user.neighborhoods.first.name %><br>

<%= link_to "Edit your profile", edit_user_path(@user) %>

<p><%= button_to 'Delete your account', @user, method: :delete %></p>

<div class="photos">
  <% @user.items.each do |item| %>
    <div class="photo" id="<%= item.id %>">
      <i class="glyphicon glyphicon-remove"></i>
      <%= image_tag item.photo.url(:square) %><br>
      <span><%= item.name %></span><br>
    </div>
  <% end %>
</div>

<div id="cover"></div>

<iframe id="edit_frame" src=""></iframe>

<h2>Mutual Likes</h2>
<ul>
  <% mutual_users = @user.inverse_liked_users.select {
    |user| @user.liked_users.include?(user) } %>
  <% mutual_users.each do |user| %>
    <li><%= link_to user.username, user %>
      <div class="hide_like" a ref="#"> <%#= button_to "Hide", destroy_like_path %></div></li>
  <% end %>
</ul>

<h2>Likes My Wagon</h2>
<ul>
  <% current_user.inverse_liked_users.each do |user| %>
    <li><%= link_to user.username, user %>
     <div class="hide_like" data-like-id="
     <% user.likes.each do |like| %>
       <%= like.liked_user_id if like.liked_user_id == current_user.id %>
     <% end %>"> 
  <%= button_to "Hide" %>
    </div>
  </li>
  <% end %>
</ul>

<!-- <h2>Wagons I Like</h2>

  <ul>
  <%# current_user.liked_users.each do |user| %>
    <li><%#= link_to user.username, user %></li>
    <%# end %>
  </ul> -->
<!-- 

     select from user.likes  where liked_user.id matches user.id

   @user.likes.select { |users| @liked} -->
