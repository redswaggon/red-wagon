<form class="navbar-form navbar-right">
  <%= link_to "Logout", destroy_session_path, {method: :delete, class: "btn btn-info"} %>
</form>
<p class="navbar-text navbar-right"><% if @user.neighborhoods.first.name %><%= @user.neighborhoods.first.name %><% end %></p>