<form class="navbar-form navbar-right">
  <%= link_to "Sign out", signout_path, id: "sign_out", class: "btn btn-info" %>
</form>
<p class="navbar-text user-nav-text navbar-right"><strong><%= current_user.name %></strong></p>
<p class="navbar-text hood-text navbar-right"><% if !current_user.neighborhoods.empty? %><%= current_user.neighborhoods.first.name %><% end %></p>